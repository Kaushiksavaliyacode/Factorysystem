
{
  "appId": "com.rdms.dispatch",
  "appName": "RDMS Dispatch",
  "webDir": "dist",
  "server": {
    "androidScheme": "https"
    // To make this an "Auto-Updating" APK that loads from the web:
    // Uncomment the line below and replace with your actual deployed URL (e.g. Vercel/Firebase)
    // "url": "https://your-production-url.com",
    // "cleartext": true
  },
  "plugins": {
    "SplashScreen": {
      "launchShowDuration": 2000,
      "backgroundColor": "#4f46e5",
      "showSpinner": false
    }
  }
}
